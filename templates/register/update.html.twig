
{% extends 'menuAdminUser.html.twig' %}


{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('css/style_adform.css') }}">
{% endblock %}



{% block body %}
{{form_start(form)}}
{{form_errors(form)}}
<div class="container">

    <div class="mx-auto">
        {% for message in app.session.flashbag.get('message') %}
        <span class="alert alert-{{ app.session.get('statut')}}">
            {{message}}
        </span>
        {% endfor %}
    </div>

    <div class="row no-gutters">

        <div class="row col-sm-12 bg-warning border border-primary rounded mx-auto">
        
            <div class="col-sm-3 offset-md-2 text-center">

                {% if user.eleve %}
                    <h1 class="">MODIFIER LE COMPTE DE {{user.eleve}}</h1>

                {% else %}
                    <h1>MODIFIER UN UTILISATEUR</h1>
                {% endif %}
                

            </div>

            <div class="col-sm-3 offset-4 mb-2">
                {{form_row(form.createUser,{'attr':{'class':'modifier_user'}})}}
            </div>
        
        </div> 

        <div class="row col-sm-12 cadrep mx-auto">

                <div class="form-group mx-auto w-50">


                
                {{form_label(form.username, "Identifiant :", {'label_attr': {'class': 'h5 mt-3'}}) }}
                {{mdpAleatoire}}
                {{form_widget(form.username,{'attr':{'class':'form-control w-100'}})}}

                {{form_label(form.roles, "Rôles :", {'label_attr': {'class': 'h5 mt-3'}}) }}
                {{form_widget(form.roles,{'attr':{'class':'form-control'}})}}

                {{form_end(form)}} 
                </div>

        </div>

    </div>





</div>

{% endblock body %}
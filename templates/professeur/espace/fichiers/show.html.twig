{% extends 'base.html.twig' %}

{% block title %}Fichiers{% endblock %}

{% block body %}
    <h1>Fichiers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fichier.id }}</td>
            </tr>
            <tr>
                <th>TitreFichier</th>
                <td>{{ fichier.titreFichier }}</td>
            </tr>
            <tr>
                <th>DescrFichier</th>
                <td>{{ fichier.descrFichier }}</td>
            </tr>
            <tr>
                <th>Fichier</th>
                <td>{{ fichier.fichier }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fichiers_index') }}">back to list</a>

    <a href="{{ path('fichiers_edit', {'id': fichier.id}) }}">edit</a>

    {{ include('fichiers/_delete_form.html.twig') }}
{% endblock %}
